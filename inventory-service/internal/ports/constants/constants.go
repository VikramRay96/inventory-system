package constants

// route groups and url contexts
const (
	API               = "/api"
	VERSION_V1        = "/v1"
	VERSION_V2        = "/v2"
	HEALTH_CHECK_PATH = "inventory-service/api/health"
)

// server port
const (
	SERVER_PORT = "server.port"
)
